	  Optimal solution of power(a,b) of a number using recursion
	  
	  void power(int a, int b) 
	  {      
	    if(b==0)      
	     {  
	       return 1;
	     }
	 	    if(b==0)      
	     {   
	        return a;       
	     }
		    int ans= power(a,b/2);
		    if (b%2==0)
	    {
	      return ans*ans;
	    };
		    if(b%2==1)
	    {
	      return a*ans*ans;
	    };
	  }    
